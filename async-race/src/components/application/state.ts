import { IState } from '../types/Types';

let state: IState = {
  page: 1,
  pageWin: 1,
  sort: 'id',
  order: 'ASC',
  countCar: 0,
  selectCar: 0.5,
  test: null,
  isWinner: false,
};
export default state;
